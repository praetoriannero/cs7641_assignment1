Steps to re-create experiments:
1. Install conda 23.7.4 for Ubuntu 22.04
2. Run `conda env create -f environment.yml`
3. Run `python setup.py develop`
4. Run `python a1src/main.py --run-all` to generate the "results" directory
